initSidebarItems({"struct":[["Rectangle","A simple rectangle. Not much to talk about."],["Stack","Handles focus tracking on a workspace. `focus` keeps track of the focused window's id and `up` and `down` are the windows above or below the focus stack respectively."],["Workspace","Represents a single workspace with a `tag` (name), `id`, a `layout` and a `stack` for all windows. A workspace is in charge of all windows belonging to that workspace. At each time, a single screen holds one workspace, while all the other workspaces are hidden in the background, while still being managed."]]});